import React from "react"
import layoutStyles from "./layout.css"


export default ({children}) => (
    <div>
        <header>

        </header>

        <main>

        </main>

        <footer>
            
        </footer>
    </div>
)